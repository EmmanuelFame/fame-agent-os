# Routing

Central scoring recognizes mechanical work, implementation complexity, ambiguity, architectural language, and risk domains. Budgets alter F3/F4 thresholds. Risk raises review requirements, while an approved pattern can lower the implementation tier. Tier limits block rather than silently downgrade safety.
